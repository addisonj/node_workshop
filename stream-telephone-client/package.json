{
  "name": "stream-telephone-client",
  "version": "1.0.0",
  "description": "A bit of a helper library to play telephone with streams",
  "main": "lib/client.js",
  "scripts": {
    "compile": "babel src -d lib",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13"
  },
  "dependencies": {
    "ws": "^1.0.1"
  }
}
